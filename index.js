function generatePassword(lenght, includeUppercase, includeLowercase, includeNumbers, includeSymbols) {
  
  return ``;
}

const passwordLenght = 10;
const includeUppercase = true;
const includeLowercase = true;
const includeNumbers = true;
const includeSymbols = true;

const password = generatePassword(passwordLenght, includeUppercase, includeLowercase, includeNumbers, includeSymbols);

console.log(`Your Password : ${password}`);
